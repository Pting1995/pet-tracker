<h1>What to eat?!</h1>

<h2>Foods that I haven't eaten!</h2>

<ul>
  {{#each foods}}
    {{#unless eaten}}
      {{> foods/food-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Foods that I have eaten!</h2>

<ul>
  {{#each foods}}
    {{#if eaten}}
      {{> foods/food-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Food!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">What type of food?</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">Have you eaten it?</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten!<br>
    <input type="radio" name="notEaten" value="0"> Not Consumed
  </div>

  <button type="submit">Add Food</button>
</form>
