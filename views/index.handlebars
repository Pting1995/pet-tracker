<h1>What to eat?!</h1>

<h2>Foods that I haven't eaten!</h2>

<ul>
  {{#each food}}
    {{#unless eaten}}
      {{> foods/food-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Foods that I have eaten!</h2>

<ul>
  {{#each food}} 
    {{#if eaten}}
      {{> foods/food-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Food!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="comida">What type of food?</label>
    <input type="text" id="comida" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">Have you eaten it?</label><br>
    <input type="radio" name="eaten" value="0" checked> Not Consumed<br>
    <input type="radio" name="eaten" value="1"> Has been eaten!
  </div>

  <button type="submit">Add Food</button>
</form>
